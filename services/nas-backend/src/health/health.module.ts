import { Module } from '@nestjs/common';

/**
 * Placeholder health module for the scaffold commit.
 *
 * The full health controller + service land in the next commit
 * following strict TDD (RED → GREEN → REFACTOR).
 */
@Module({})
export class HealthModule {}
