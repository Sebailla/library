/**
 * Public surface for `@alejandria/sidecar`.
 *
 * Implementation lands in a follow-up commit
 * (`feat(sidecar): implement sanitizePath + spawnSidecar`).
 */

// No exports yet — the package skeleton only. Tests live in
// `test/sidecar-process.spec.ts` and will fail RED until the
// next commit lands.
export {}